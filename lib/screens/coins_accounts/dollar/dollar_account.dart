import 'package:flutter/material.dart';

class DollarAccount extends StatelessWidget {
  const DollarAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
           title: Text('Dollar'),
      ),
    );
  }
}